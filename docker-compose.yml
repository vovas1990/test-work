version: "3"
services:
  prometheus:
    image: prom/prometheus
    restart: always
    container_name: prometheus
    ports:
    - 9090:9090
    volumes:
    - prometheus:/etc/prometheus
volumes:
  prometheus:
    external: true
